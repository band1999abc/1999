以前決めた6項目のうち、現在まだ完了していない項目を整理して、残りの作業を進めたいです。

現在の状態は以下です。

【完了】
1. Music Playsのカウント条件
- 明示的なPlay操作で実際に再生が開始された場合のみ music_play
- Previewで実際の再生と1回だけの記録を確認済み
- Productionにも反映済み
→ 完了扱い。変更しない。

【修正済み・最終確認が必要】
2. 「ひび割れ」歌詞ページのクジラ位置
- 位置調整は実装済み
- サイズ、アニメーション、速度、デザインは変更しない
→ 現在の実装を確認し、問題なければ変更しない。

5. Node.js 20 → 24
- package.json / Vercel側ともNode 24.xへ変更済み
→ 実際のPreview/ProductionのBuild RuntimeがNode 24.xになっているか確認する。
→ 問題なければコード変更はしない。

【まだ完了していない】
3. Analytics / Insights / Milestonesの初期化
4. Diary / Liveのページネーション
6. 不要なスクロール・画面枠の修正

まず、この3項目について現在のコード・データ・Production/Preview設定を調査してください。

━━━━━━━━━━━━━━━━━━
【3. Analytics / Insights / Milestones】
━━━━━━━━━━━━━━━━━━

目的：
Productionを正式運用開始時点から0として扱える状態にする。

対象：
- Visitors
- New Visitors
- Returning Visitors
- Music Plays
- QR
- Pages
- Release Impact
- Insights
- Milestones

ただし、これは本番データを削除する作業なので非常に慎重に扱ってください。

必ず先に、
- 現在どのデータがProduction由来なのか
- Preview / 開発 / テスト由来のデータと区別できるのか
- Upstash Redis/KVのどこに保存されているのか
- Milestonesの保存場所
- Productionで実際に使用されているデータストア
を調査してください。

データの削除・初期化はまだ実行しないでください。

また、現在のmusic_play変更との整合性も確認してください。

特に、
- event definitions
- aggregation
- Insights
- Release Impact
- Milestones
が壊れないことを確認してください。

調査結果と、初期化するなら具体的に何を削除・リセットするのかを報告してください。

━━━━━━━━━━━━━━━━━━
【4. Diary / Live ページネーション】
━━━━━━━━━━━━━━━━━━

以前の要件：

- 新しい投稿が1ページ目
- 1 2 3 → のようなページナビゲーション
- 1ページしかない場合はナビゲーションを表示しない
- デザイン・操作感を統一
- 既存データ、URL、CRUDを壊さない
- スマホ / タブレット / PC対応

まず現在の実装を調査してください。

特に確認すること：

- After HoursのDiary / Live管理画面だけのページネーションなのか
- 公開Diary / Liveページにもページネーションが必要なのか
- Diaryは新しい投稿が本当に1ページ目に来るか
- Liveは現在のsort_orderの仕組みにより新しい投稿が1ページ目に来ない問題があるか
- APIが全件返しているのか
- ページ単位で取得する設計なのか
- 既存URLやCRUDへの影響

要件を満たしていない部分があれば、具体的な修正案を提示してください。

この段階ではまだ修正・Commit・Pushはしないでください。

━━━━━━━━━━━━━━━━━━
【6. 不要なスクロール・画面枠】
━━━━━━━━━━━━━━━━━━

目的：

画面内に収まるページでは不要な縦・横スクロールを発生させない。

ただし、
- Diary
- Music
- その他コンテンツ量が多いページ
など、本当に長いページでは必要なスクロールを残す。

対象候補：
- style.css
- admin.css
- live.js
- diary.js
- music.js
- track.js
- その他、実際に原因となっているファイル

特に確認：
- iPhone Safari
- iPad Safari
- PC
- 横方向の1〜数px程度の不要なoverflow
- viewport高さの扱い
- fixed / sticky要素
- body / htmlのoverflow
- After Hoursの画面
- モーダル表示時のスクロール

既存デザイン、余白、クジラ、アニメーションなどを不用意に変更しないでください。

まず原因を特定し、
「どのページのどの要素が原因で、どう直すのが安全か」
を報告してください。

━━━━━━━━━━━━━━━━━━
【2. クジラ / 5. Node.js 24 の確認】
━━━━━━━━━━━━━━━━━━

2については、
- hibiware-specific CSS
- whale.js
- 他ページとの位置関係
を確認。

5については、
- package.json
- package-lock.json
- Vercel Project Settings
- Vercel DeploymentのBuild Runtime
- Node.js runtime依存のAPI / Function
を確認。

問題がなければ変更しない。

━━━━━━━━━━━━━━━━━━
【重要な作業ルール】
━━━━━━━━━━━━━━━━━━

今回はまず「調査・設計」までです。

まだ以下は実行しないでください。

- GitHub Push
- Production Deployment
- Productionデータ削除
- Analyticsデータ初期化
- Commit
- Rollback
- 環境変数変更

また、今回の6項目以外の改善やリファクタリングを勝手に行わないでください。

最後に、

1. 完了
2. 確認のみ必要
3. 修正が必要
4. 本番データ操作が必要

の4分類で結果を整理し、

各項目について
- 現状
- 問題点
- 修正方法
- 影響範囲
- Productionデータへの影響
- 次に実行すべき作業

を報告してください。